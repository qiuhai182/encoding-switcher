# 更新日志

## 0.10.1

- 修复：按特定编码保存后，底部状态栏编码显示停留在旧编码——转换重开验证若走「显示已正确」早退路径（内容匹配但文档模型编码仍是旧值），现在会补一次指定编码重开，强制状态栏标签与磁盘编码同步

## 0.10.0

- 新增：AI 新建文件的初始编码归一
  - 新建文件落盘后自动检测编码，与本平台 `files.encoding` 设置不一致时无损转换为设置默认编码（回读校验 + 行数校验，失败不动文件）
  - 多平台隔离（Trae/Cursor/VSCode 同开工作区）：文件系统不记录写者身份，采用抢占式处理权登记（claim，写系统临时目录，跨平台可见）实现等效隔离——同一新文件只有先抢到登记的实例执行归一，其它平台实例让路；归一结果 60 秒内所有实例的编码迁移回滚接受现状，避免多平台按各自默认编码来回转换打架
  - 安全约束：仅处理文本扩展名（防误改二进制）、空文件/半截写入/纯 ASCII/无法识别编码均跳过、含不可解码字节放弃归一

## 0.9.1

- 修复编码兜底视图（encoding-view）三处隐患：
  - 视图保存防线补上跨族编码变化拦截：GBK 视图遇到磁盘已变回 UTF-8（或反向）时阻止保存——此前该场景放行，会把视图里的乱码文本以新编码写回，把好文件真正写坏
  - 视图跟随磁盘编码：外部改写导致磁盘编码与视图编码不一致时，自动关闭过时视图并按磁盘真实状态重开（正常文件直接打开，仍是非 UTF 编码则换新编码视图）
  - 兜底打开视图前按磁盘最新字节重检：重开/回滚期间磁盘已恢复正常编码时不再打开错误编码视图误导用户

## 0.9.0

- 修复多开窗口时插件多实例"编码打架"（同一文件被来回转码 utf8→gbk→utf8 循环）：
  - 编码历史（上次编码记录）改为跨窗口共享（globalState），各窗口实例同步感知彼此的切换与修复结果
  - 主动切换编码时登记意图（新编码 + 磁盘指纹），其它窗口检测到指纹一致的外部变化时接受新编码，不再误判为 AI 改写而回滚
  - 熔断兜底：10 秒内同一文件被向相反方向回滚两次，判定为多实例争用，暂停自动回滚并告警一次（不依赖跨窗口通信，内容始终无损）
- 所有涉及具体文件的弹窗（自动修复/自动转回/编码切换/多实例争用告警）统一带「打开文件」按钮，一键跳到目标文件

## 0.8.2

- 修复严重误判：罗马尼亚语/法语等欧洲语言文件（几乎全 Latin 字符）被误判为 GBK 双重转码并遭"修复"损坏（如 darkreader 的 ro/messages.json 被反复改写为带 ? 的损坏版，与构建流程互相冲突）。双重防线：
  - 双重转码修复新增逆向无损校验：latin1/cp1252 逆向时任何字符被替换成 `?` 即判定为正常文本并放弃（真 mojibake 逆向是逐字节零替换的）
  - 还原后内容必须包含"成词中文"（CJK ≥3 且相邻 CJK 对 ≥2）才允许修复；GBK 随机对撞出的零星孤立汉字不再触发
  - 编码迁移回滚同样采用强 CJK 门槛，防止对撞汉字触发来回改写
- 新增 36 项单元测试（含 ro/fr 欧洲语言回归用例）

## 0.8.1

- 修复扩展详情页中文乱码：README.md 源文件被以 GBK 写入，打包时按 UTF-8 读取导致中文全部变成替换字符；已转为 UTF-8 并重新打包
- 打包排除本地辅助脚本（`check-enc.js`、`convert_to_utf8.py`）

## 0.8.0

- 新增**字节级自动修复**（`encoding-guard.autoRepairBytes`，默认开启）：
  - 双重转码还原：GBK 等编码字节经 latin1/cp1252 路径被写成 UTF-8 时，逆向转码 100% 还原
  - 混合编码分段转码：原 GBK 文件被 AI 用 UTF-8 追加/改写部分行（或对称场景）时，逐行判定编码归属，少数派行无损转成多数派编码
  - 编码迁移回滚：内容完好但编码被整体改写（GBK 文件被 AI 整体按 UTF-8 重写，或对称地 UTF-8 文件被按 GBK 重写）时，自动转回监听记录的原编码，双向支持
  - 只做纯字节转码，保证内容与行数不变（多字节序列不含 0x0A 的数学保证 + 计数校验兜底）
  - 三道硬校验（无损解码/行数不变/行级 round-trip），任一失败放弃修复退回告警
  - 并发安全：静默窗口（2 秒指纹不变才动手）+ 动手前重读比对 + 临时文件 rename 原子替换 + 回写对抗（AI 用旧快照覆盖时自动再修，上限 3 次）
  - 修复前 `.bak` 临时备份（`repairBackup`，成功后自动删除）；大文件上限 `maxRepairBytes`（默认 5MB）
- 全文一致性校验替代头部探测：AI 只改写/追加部分内容（头部合法、中部混入其他编码）现在也能检出

## 0.7.0

- 插件更名：`encoding-switcher` → `encoding-guard`（定位从"切换"升级为"自动检测 + 守护防乱码"）
- 修复正常编辑文件时误触发自动重开：
  - 编码歧义保护：显示内容与磁盘解码内容均无损坏但不同（字节存在多种自洽解码）时保守不重开
  - 二次确认：外部改写校验前延迟复核，排除保存写入中、格式化瞬间等瞬态竞态
  - 编辑状态复查：文档 isDirty 时放弃重开/关闭编辑器
- 每次重新打开文件都重新走完整检测纠正链（清除历史指纹/冷却记录），内部重开事件标记拦截，不产生循环
- 保存防覆盖：编码编辑器 Ctrl+S 时重新检测文件实际编码并按它写回，编码被外部改写时阻止保存并告警

## 0.6.0

- 编码列表配置化，不写死 GBK：新增 `detectionEncodings`、`kernelGuessEncodings`、`applyKernelGuess` 配置项，支持任意 iconv-lite 编码（Big5、Shift-JIS、CP1252 等）
- 修复损坏内核 `Files: Auto Guess Encoding` 设置的问题：默认不再改写内核编码设置；历史版本误写的全局候选列表自动检测并恢复

## 0.5.0 及更早

- 打开文件自动检测编码，内核猜错时自动按正确编码重开（命令式重开优先，失败退回关闭重开）
- 正确编码可编辑视图（FileSystemProvider）：内核不支持编码重开时兜底，可编辑、Ctrl+S 按原编码写回
- AI 改写自动纠正：比对磁盘内容与编辑器显示，不一致自动重开
- 外部写入监督：FileSystemWatcher 监听未打开文件，疑似编码损坏及时告警，双重转码给出手动还原指引
- 标题栏「选择编码保存」：按源编码解码磁盘字节再以目标编码写回，中文内容不损坏
