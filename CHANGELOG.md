# 更新日志

## 0.7.0

- 插件更名：`encoding-switcher` → `encoding-guard`（定位从"切换"升级为"自动检测 + 守护防乱码"）
- 修复正常编辑文件时误触发自动重开：
  - 编码歧义保护：显示内容与磁盘解码内容均无损坏但不同（字节存在多种自洽解码）时保守不重开
  - 二次确认：外部改写校验前延迟复核，排除保存写入中、格式化瞬间等瞬态竞态
  - 编辑状态复查：文档 isDirty 时放弃重开/关闭编辑器
- 每次重新打开文件都重新走完整检测纠正链（清除历史指纹/冷却记录），内部重开事件标记拦截，不产生循环
- 保存防覆盖：编码编辑器 Ctrl+S 时重新检测文件实际编码并按它写回，编码被外部改写时阻止保存并告警

## 0.6.0

- 编码列表配置化，不写死 GBK：新增 `detectionEncodings`、`kernelGuessEncodings`、`applyKernelGuess` 配置项，支持任意 iconv-lite 编码（Big5、Shift-JIS、CP1252 等）
- 修复损坏内核 `Files: Auto Guess Encoding` 设置的问题：默认不再改写内核编码设置；历史版本误写的全局候选列表自动检测并恢复

## 0.5.0 及更早

- 打开文件自动检测编码，内核猜错时自动按正确编码重开（命令式重开优先，失败退回关闭重开）
- 正确编码可编辑视图（FileSystemProvider）：内核不支持编码重开时兜底，可编辑、Ctrl+S 按原编码写回
- AI 改写自动纠正：比对磁盘内容与编辑器显示，不一致自动重开
- 外部写入监督：FileSystemWatcher 监听未打开文件，疑似编码损坏及时告警，双重转码给出手动还原指引
- 标题栏「选择编码保存」：按源编码解码磁盘字节再以目标编码写回，中文内容不损坏
